pi = 3.14
radius = float(input("Enter the value of radius: "))  # Convert input to float

area = pi * radius * radius

print("The area is " + str(area))  # Convert area to string for concatenation
